import type{Metadata}from"next";import{ApplicationList}from"@/components/applications/application-list";import{AccountCard,AccountPage}from"@/components/account/account-page";import{ButtonLink}from"@/components/ui/button";import{applicationStatuses,applicationStatusLabels,isApplicationStatus}from"@/lib/applications/application-status";import{getCandidateApplications}from"@/lib/applications/candidate-applications";export const metadata:Metadata={title:"Your Applications | Talvanta Africa",robots:{index:false,follow:false}};export default async function Page({searchParams}:{searchParams:Promise<{status?:string}>}){const params=await searchParams;const filter=isApplicationStatus(params.status)?params.status:null;const data=await getCandidateApplications(filter);return <AccountPage eyebrow="Candidate Account" title="Your Job Applications" intro="Track applications submitted through your Talvanta Africa candidate account."><div className="grid gap-6"><AccountCard title="Filter applications"><nav aria-label="Application status filters"><ul className="flex flex-wrap gap-2"><li><ButtonLink href="/account/candidate/applications" variant={!filter?"primary":"outline"}>All</ButtonLink></li>{applicationStatuses.map(s=><li key={s}><ButtonLink href={`/account/candidate/applications?status=${s}`} variant={filter===s?"primary":"outline"}>{applicationStatusLabels[s]}</ButtonLink></li>)}</ul></nav></AccountCard><AccountCard>{data.unavailable?<p role="status" className="mb-4 text-slate">Applications are temporarily unavailable.</p>:null}<ApplicationList items={data.items} basePath="/account/candidate/applications"/></AccountCard></div></AccountPage>}
