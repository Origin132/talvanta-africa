import type{Metadata}from"next";import{AccountCard,AccountPage}from"@/components/account/account-page";import{ReviewList}from"@/components/applications/review-list";import{ButtonLink}from"@/components/ui/button";import{applicationStatuses,applicationStatusLabels,isApplicationStatus}from"@/lib/applications/application-status";import{getEmployerApplications}from"@/lib/applications/review-applications";export const metadata:Metadata={title:"Vacancy Applications | Talvanta Africa",robots:{index:false,follow:false}};export default async function Page({searchParams}:{searchParams:Promise<{status?:string}>}){const p=await searchParams;const filter=isApplicationStatus(p.status)?p.status:null;const data=await getEmployerApplications(filter);return <AccountPage eyebrow="Employer Account" title="Vacancy Applications" intro="View applications connected only to vacancies associated with your employer account."><div className="grid gap-6"><AccountCard title="Filter applications"><div className="flex flex-wrap gap-2"><ButtonLink href="/account/employer/applications" variant={!filter?"primary":"outline"}>All</ButtonLink>{applicationStatuses.map(s=><ButtonLink key={s} href={`/account/employer/applications?status=${s}`} variant={filter===s?"primary":"outline"}>{applicationStatusLabels[s]}</ButtonLink>)}</div></AccountCard><AccountCard>{data.unavailable?<p role="status" className="mb-4 text-slate">Applications are temporarily unavailable.</p>:null}<ReviewList items={data.items} basePath="/account/employer/applications"/></AccountCard><p className="text-sm text-slate">Candidate contact and document access will be introduced through a controlled recruitment-stage workflow.</p></div></AccountPage>}
