import type {Metadata} from "next"; import {AccountCard,AccountPage} from "@/components/account/account-page"; import {RequestList} from "@/components/account/employer/request-list"; import {ButtonLink} from "@/components/ui/button"; import {getEmployerAccount} from "@/lib/employers/get-employer-account"; import {activeStatuses} from "@/lib/employers/recruitment-request-status"; import {employerCompletion,nextEmployerProfileStep} from "@/lib/profiles/profile-completion";
export const metadata:Metadata={title:"Employer Dashboard | Talvanta Africa",robots:{index:false,follow:false}};
export default async function Page(){const data=await getEmployerAccount();const completion=employerCompletion(data.current.profile,data.employer);const active=data.requests.filter(r=>activeStatuses.includes(r.status)).length;const drafts=data.requests.filter(r=>r.status==="draft").length;const quick=[["Create Recruitment Request","/account/employer/requests/new"],["View Recruitment Requests","/account/employer/requests"],["Candidate Applications","/account/employer/applications"],["Edit Organisation Profile","/account/employer/profile/edit"],["Explore Recruitment Services","/services"],["Contact Talvanta Africa","/contact"]]as const;return <AccountPage eyebrow="Employer Account" title={`Welcome back, ${data.current.profile.full_name}`} intro="Manage your organisation profile and recruitment requests from one secure account area."><div className="grid gap-6"><div className="grid gap-5 md:grid-cols-2 xl:grid-cols-4"><AccountCard title="Organisation Profile"><p className="text-3xl font-extrabold text-navy">{completion}%</p><p className="mt-2 text-sm text-slate">{data.employer?.organisation_name??"Organisation not provided"}<br/>{data.employer?.organisation_location??"Location not provided"}<br/>{data.employer?.contact_role??"Contact role not provided"}</p><p className="mt-2 text-sm text-slate">Next: {nextEmployerProfileStep(data.current.profile,data.employer)}. Completion is guidance, not verification.</p><ButtonLink className="mt-4 w-full" href="/account/employer/profile" variant="outline">Review Organisation Profile</ButtonLink></AccountCard><AccountCard title="Active Requests"><p className="text-3xl font-extrabold text-navy">{active}</p><p className="mt-2 text-slate">{active?`${active} active recruitment request${active===1?"":"s"}`:"No active recruitment requests"}</p><ButtonLink className="mt-4 w-full" href={active?"/account/employer/requests":"/account/employer/requests/new"} variant="outline">{active?"View Requests":"Create Recruitment Request"}</ButtonLink></AccountCard><AccountCard title="Draft Requests"><p className="text-3xl font-extrabold text-navy">{drafts}</p><ButtonLink className="mt-4 w-full" href="/account/employer/requests?status=draft" variant="outline">View Drafts</ButtonLink></AccountCard><AccountCard title="Account Status"><p className="text-slate">{data.current.user.email_confirmed_at?"Email verified":"Email verification required"}</p>{data.current.profile.onboarding_completed?<p className="mt-2 text-slate">Employer profile active</p>:null}</AccountCard></div><AccountCard title="Recent Recruitment Requests">{data.requestsUnavailable?<p className="text-slate">We could not load your recruitment requests at this time.</p>:<RequestList requests={data.requests.slice(0,5)}/>}</AccountCard><div className="grid gap-6 lg:grid-cols-2"><AccountCard title="Recent Account Information"><dl className="space-y-3"><div><dt className="font-bold text-navy">Account created</dt><dd className="text-slate">{new Date(data.current.profile.created_at).toLocaleDateString("en-NG")}</dd></div><div><dt className="font-bold text-navy">Organisation profile updated</dt><dd className="text-slate">{new Date(data.employer?.updated_at??data.current.profile.updated_at).toLocaleDateString("en-NG")}</dd></div>{data.requests[0]?<><div><dt className="font-bold text-navy">Most recent request created</dt><dd className="text-slate">{new Date(data.requests[0].created_at).toLocaleDateString("en-NG")}</dd></div><div><dt className="font-bold text-navy">Most recent request updated</dt><dd className="text-slate">{new Date(data.requests[0].updated_at).toLocaleDateString("en-NG")}</dd></div></>:null}</dl><p className="mt-3 text-sm text-slate">This is not a complete audit log.</p></AccountCard><AccountCard title="Quick Actions"><ul className="grid gap-3 sm:grid-cols-2">{quick.map(([label,href])=><li key={href}><ButtonLink className="w-full" href={href} variant="outline">{label}</ButtonLink></li>)}</ul></AccountCard></div></div></AccountPage>}
