import type{Metadata}from"next";import{redirect}from"next/navigation";import{AccountCard,AccountPage}from"@/components/account/account-page";import{RecruitmentRequestForm}from"@/components/account/employer/recruitment-request-form";import{editableStatuses}from"@/lib/employers/recruitment-request-status";import{getOwnedRequest}from"@/lib/employers/get-employer-account";export const metadata:Metadata={title:"Edit Recruitment Request | Talvanta Africa",robots:{index:false,follow:false}};export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params;const{request}=await getOwnedRequest(id);if(!editableStatuses.includes(request.status))redirect(`/account/employer/requests/${id}`);return <AccountPage eyebrow="Employer Account" title={request.status==="draft"?"Edit Draft Recruitment Request":"Update Recruitment Request"} intro={request.status==="draft"?"Update the draft, save your changes, or submit it for review.":"Provide the requested clarification, save changes, or resubmit for review."}><AccountCard><RecruitmentRequestForm request={request} organisationName={request.organisation_name}/></AccountCard></AccountPage>}
