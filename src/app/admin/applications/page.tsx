import type{Metadata}from"next";import{AccountCard,AccountPage}from"@/components/account/account-page";import{ReviewList}from"@/components/applications/review-list";import{AdminAccessMessage}from"@/components/admin/admin-access-message";import{ButtonLink}from"@/components/ui/button";import{applicationStatuses,applicationStatusLabels,isApplicationStatus}from"@/lib/applications/application-status";import{getAdminApplications}from"@/lib/applications/review-applications";export const metadata:Metadata={title:"Application Review | Talvanta Africa",robots:{index:false,follow:false}};export default async function Page({searchParams}:{searchParams:Promise<{status?:string}>}){const p=await searchParams;const filter=isApplicationStatus(p.status)?p.status:null;const data=await getAdminApplications(filter);if(data.status!=="ready")return <AdminAccessMessage status={data.status}/>;return <AccountPage eyebrow="Administration" title="Job Applications" intro="Review candidate applications and apply controlled recruitment-stage updates."><div className="grid gap-6"><AccountCard title="Filter applications"><div className="flex flex-wrap gap-2"><ButtonLink href="/admin/applications" variant={!filter?"primary":"outline"}>All</ButtonLink>{applicationStatuses.map(s=><ButtonLink key={s} href={`/admin/applications?status=${s}`} variant={filter===s?"primary":"outline"}>{applicationStatusLabels[s]}</ButtonLink>)}</div></AccountCard><AccountCard><ReviewList items={[...data.items]} basePath="/admin/applications"/></AccountCard></div></AccountPage>}
