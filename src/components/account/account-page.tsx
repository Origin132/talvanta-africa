import type { ReactNode } from "react";
export function AccountPage({ eyebrow, title, intro, children }: { eyebrow: string; title: string; intro: string; children: ReactNode }) { return <section className="py-10 sm:py-14"><p className="font-bold uppercase tracking-[0.16em] text-green">{eyebrow}</p><h1 className="mt-2 font-heading text-3xl font-extrabold text-navy sm:text-4xl">{title}</h1><p className="mt-4 max-w-3xl leading-7 text-slate">{intro}</p><div className="mt-8">{children}</div></section>; }
export function AccountCard({ title, children }: { title?: string; children: ReactNode }) { return <section className="rounded-[var(--radius)] border border-border-grey bg-white p-5 shadow-sm sm:p-7">{title ? <h2 className="font-heading text-xl font-extrabold text-navy">{title}</h2> : null}<div className={title ? "mt-4" : ""}>{children}</div></section>; }
