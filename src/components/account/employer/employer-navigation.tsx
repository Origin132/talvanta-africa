"use client";
import Link from "next/link"; import { usePathname } from "next/navigation";
const items = [{ label: "Overview", href: "/account/employer", active: (p:string)=>p==="/account/employer" }, { label: "Organisation Profile", href: "/account/employer/profile", active:(p:string)=>p.startsWith("/account/employer/profile") }, { label: "Recruitment Requests", href: "/account/employer/requests", active:(p:string)=>p.startsWith("/account/employer/requests") }, { label: "Settings", href: "/account/settings", active:(p:string)=>p.startsWith("/account/settings") }] as const;
export function EmployerNavigation(){const path=usePathname();return <nav aria-label="Employer account navigation" className="border-b border-border-grey"><ul className="flex gap-1 overflow-x-auto py-3">{items.map(item=>{const active=item.active(path);return <li key={item.href} className="shrink-0"><Link href={item.href} aria-current={active?"page":undefined} className={`inline-flex min-h-11 items-center rounded-lg px-4 font-bold focus-visible:outline-3 focus-visible:outline-offset-2 focus-visible:outline-gold ${active?"bg-navy text-white":"text-navy hover:bg-white hover:text-green"}`}>{item.label}</Link></li>})}</ul></nav>}
