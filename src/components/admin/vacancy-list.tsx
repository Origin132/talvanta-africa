import { ButtonLink } from "@/components/ui/button";
import type { Vacancy } from "@/lib/supabase/database.types";
import { vacancyStatusLabels } from "@/lib/vacancies/vacancy-status";
const date=(v:string|null)=>v?new Intl.DateTimeFormat("en-NG",{dateStyle:"medium",timeZone:"Africa/Lagos"}).format(new Date(v)):"Not available";
export function VacancyList({vacancies}:{vacancies:Vacancy[]}){if(!vacancies.length)return <p className="leading-7 text-slate">No vacancies match this view.</p>;return <div className="grid gap-4">{vacancies.map(v=><article key={v.id} className="rounded-[var(--radius)] border border-border-grey bg-white p-5 shadow-sm"><div className="flex flex-wrap items-start justify-between gap-3"><div><h2 className="break-words font-heading text-xl font-extrabold text-navy">{v.job_title}</h2><p className="mt-1 text-slate">{v.organisation_name}</p></div><span className="rounded-full border border-border-grey bg-soft-grey px-3 py-1 text-sm font-bold text-navy">Status: {vacancyStatusLabels[v.status]}</span></div><dl className="mt-5 grid gap-3 text-sm sm:grid-cols-2 lg:grid-cols-4"><D t="Location" v={v.job_location}/><D t="Employment" v={v.employment_type}/><D t="Workplace" v={v.workplace_type}/><D t="Positions" v={String(v.number_of_positions)}/><D t="Published" v={date(v.published_at)}/><D t="Closing" v={date(v.closes_at)}/><D t="Applications" v={v.applications_open?"Open":"Not open"}/></dl><div className="mt-5 flex flex-wrap gap-3"><ButtonLink href={`/admin/vacancies/${v.id}`}>View</ButtonLink>{["draft","published","closing-soon"].includes(v.status)?<ButtonLink href={`/admin/vacancies/${v.id}/edit`} variant="outline">Edit</ButtonLink>:null}</div></article>)}</div>}
function D({t,v}:{t:string;v:string}){return <div><dt className="font-bold text-navy">{t}</dt><dd className="break-words text-slate">{v}</dd></div>}
