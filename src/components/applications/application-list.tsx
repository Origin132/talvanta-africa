import { ButtonLink } from "@/components/ui/button";
import { ApplicationStatus } from "@/components/applications/application-status";
import type { CandidateApplicationView } from "@/lib/applications/candidate-applications";
const date=(value:string)=>new Intl.DateTimeFormat("en-NG",{dateStyle:"medium",timeZone:"Africa/Lagos"}).format(new Date(value));
export function ApplicationList({items,basePath}:{items:Array<Pick<CandidateApplicationView,"application"|"vacancy">>;basePath:string}){if(!items.length)return <p className="text-slate">No applications match this view.</p>;return <div className="grid gap-4">{items.map(({application,vacancy})=><article key={application.id} className="rounded-[var(--radius)] border border-border-grey p-5"><div className="flex flex-wrap items-start justify-between gap-3"><div><h2 className="break-words font-heading text-xl font-extrabold text-navy">{vacancy.job_title}</h2><p className="text-slate">{vacancy.organisation_name}</p></div><ApplicationStatus status={application.status}/></div><dl className="mt-4 grid gap-3 text-sm sm:grid-cols-3"><div><dt className="font-bold text-navy">Location</dt><dd className="text-slate">{vacancy.job_location}</dd></div><div><dt className="font-bold text-navy">Employment</dt><dd className="text-slate">{vacancy.employment_type}</dd></div><div><dt className="font-bold text-navy">Submitted</dt><dd className="text-slate">{date(application.submitted_at)}</dd></div></dl><ButtonLink className="mt-5" href={`${basePath}/${application.id}`}>View Application</ButtonLink></article>)}</div>}
