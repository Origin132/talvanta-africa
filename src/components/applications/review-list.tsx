import{ApplicationStatus}from"@/components/applications/application-status";import{ButtonLink}from"@/components/ui/button";import type{ReviewItem}from"@/lib/applications/review-applications";const date=(v:string)=>new Intl.DateTimeFormat("en-NG",{dateStyle:"medium",timeZone:"Africa/Lagos"}).format(new Date(v));export function ReviewList({items,basePath}:{items:ReviewItem[];basePath:string}){if(!items.length)return <p className="text-slate">No applications match this view.</p>;return <div className="grid gap-4">{items.map(({application:a,vacancy:v,candidate:c})=><article key={a.id} className="rounded-[var(--radius)] border border-border-grey p-5"><div className="flex flex-wrap items-start justify-between gap-3"><div><h2 className="break-words text-xl font-extrabold text-navy">{c.full_name}</h2><p className="text-slate">{v.job_title} · {v.organisation_name}</p></div><ApplicationStatus status={a.status}/></div><dl className="mt-4 grid gap-3 text-sm sm:grid-cols-3"><D t="Submitted" v={date(a.submitted_at)}/><D t="Location" v={c.current_location??"Not provided"}/><D t="CV attached" v={a.candidate_document_id?"Yes":"No"}/></dl><ButtonLink className="mt-5" href={`${basePath}/${a.id}`}>View Application</ButtonLink></article>)}</div>}function D({t,v}:{t:string;v:string}){return <div><dt className="font-bold text-navy">{t}</dt><dd className="text-slate">{v}</dd></div>}
