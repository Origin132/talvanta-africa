import{ButtonLink}from"@/components/ui/button";import type{PublicVacancy}from"@/lib/vacancies/public-vacancies";import{vacancyStatusLabels}from"@/lib/vacancies/vacancy-status";const date=(v:string|null)=>v?new Intl.DateTimeFormat("en-NG",{dateStyle:"medium",timeZone:"Africa/Lagos"}).format(new Date(v)):"Not available";
export function JobCard({job}:{job:PublicVacancy}){return <article className="flex h-full flex-col rounded-[var(--radius)] border border-border-grey bg-white p-6 shadow-sm sm:p-7"><div className="flex flex-wrap items-start justify-between gap-3"><div className="min-w-0"><p className="break-words text-sm font-bold text-green">{job.organisation_name}</p><h2 className="mt-2 break-words text-2xl font-extrabold leading-tight text-navy">{job.job_title}</h2></div><span className="rounded-full border border-border-grey bg-soft-grey px-3 py-1 text-xs font-extrabold uppercase tracking-wide text-navy">Status: {vacancyStatusLabels[job.status]}</span></div><dl className="mt-6 grid grid-cols-2 gap-4 text-sm"><D t="Location" v={job.job_location}/><D t="Workplace" v={job.workplace_type}/><D t="Employment" v={job.employment_type}/><D t="Positions" v={String(job.number_of_positions)}/>{job.salary_range?<D t="Salary" v={job.salary_range}/>:null}<D t="Published" v={date(job.published_at)}/><D t="Closing" v={date(job.closes_at)}/></dl><p className="mt-6 flex-1 leading-7 text-slate">{job.role_summary}</p><div className="mt-7"><ButtonLink href={`/jobs/${job.slug}`} variant="outline">View {job.job_title}</ButtonLink></div></article>}
function D({t,v}:{t:string;v:string}){return <div className="min-w-0"><dt className="font-bold text-navy">{t}</dt><dd className="mt-1 break-words text-slate">{v}</dd></div>}
